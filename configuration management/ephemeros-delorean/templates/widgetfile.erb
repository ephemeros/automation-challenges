# Template for widgetfile using the module's paramters and the fact widget to generate the file

option <%= @option %>
speed <%= @speed %>
capacitor_type <%= @capacitor %>
widget_type <%= @widget %>
model <%= @model %>
avoid <%= @avoid %>
